# 普通攻击基础伤害公式 V1

> 状态：当前最适合本仓库使用的战报逆向候选（2026-08-14）。
>
> 这不是官方公开公式，也不是已验证的服务器源码。本文只覆盖已清洗的普通攻击基础伤害层；会心、战法附加伤害、未识别状态和击杀封顶必须在外层独立处理。

## 输入与适用范围

- `N`：攻击者**实际出手瞬间**的当前兵力，不能使用回合开始快照。
- `W`：攻击者本次攻击时的当前武力。
- `T`：防守者本次受击时的当前统率。
- `La`、`Ld`：攻守双方等级。当前样本只充分覆盖 `L >= 20`。
- `K`：兵种克制倍率；当前已验证 `枪 -> 骑 = 1.12`、`骑 -> 枪 = 0.88`，其余已检验无克制组合为 `1`。
- `Ms`：已确认的士气及其他外层伤害倍率。纯净样本取 `1`。

击杀伤害（理论伤害高于目标受击前剩余兵力）不应用于反推本公式；在结算层再单独封顶。

## 最终候选

令 `ceil(x)` 为向上取整，`round(x) = floor(x + 0.5)`（仅用于非负数）。

```text
Sa = 0.6 + 0.02 * La
Sd = 0.6 + 0.02 * Ld

X  = F(N) + W * Sa - T * Sd
M  = min(100, ceil(N / 50))
B0 = ceil(max(X, M))
B1 = ceil(B0 * K)
B2 = ceil(B1 * Ms)

D0(R) = ceil(B2 * R / 100),  R in {86, 87, ..., 94}
DamageBase = max(D0(R), U),    U in {5, 6, ..., 15}
DamageShown = min(DamageBase, defender_troops_before_hit)
```

`DamageBase` 是本公式所解释的普通攻击基础结果；`DamageShown` 是加入击杀封顶后的显示伤害。对一次预测，应输出 `R=86..94` 的九个离散候选值，而不是凭空抽取连续随机数。

### 士气

在已知“每低 1 点士气降低 0.7% 伤害”的条件下：

```text
M_morale = 1 - 0.007 * max(0, 100 - morale)
```

可将其作为 `Ms` 的已确认组成部分。多个增伤、减伤、士气和克制层之间是否全部独立 `ceil`，仍需要单效果控制样本继续确认；不要把未经验证的效果直接并入 `F(N)`。

## 兵力函数 F(N)

### 逆向候选来源

当前完整查表中的数值来源于以下候选结构：

```text
N <= 2000:
    F(N) = ceil(N / 10) + ceil(N / 50)

2000 < N < 5000:
    F(N) = T_mid[N]

N >= 5000:
    F(N) = round(429.27105 + 100 * log2(N / 5000))
```

低段的两个 `ceil` 必须分别计算，不能合并为 `ceil(0.12 * N)`。中段 `T_mid` 是由本地战报得到的 2,999 项单调查表经验候选。

### 运行时实现

战斗运行时**不再按上述三个区段即时计算 `F(N)`**。`N=1..10000` 的全部结果已经固化在：

[`data/normal_attack/troop_function_table_1_10000.csv`](data/normal_attack/troop_function_table_1_10000.csv)

运行时统一执行：

```text
F(N) = T_full[N],  N in {1, 2, ..., 10000}
```

也就是说，低段、中段和高段全部通过同一张完整 CSV 查表。表外兵力不会回退到近似式或高段公式，而应显式报错，直到完整表的支持范围被正式扩展。

关键边界包括：`F(1)=2`、`F(2000)=240`、`F(2001)=238`、`F(4999)=429`、`F(5000)=429`、`F(10000)=529`。

原来的中段单独文件 [`data/normal_attack/mid_troop_table_2001_4999.csv`](data/normal_attack/mid_troop_table_2001_4999.csv) 仍可作为数据来源和校验依据，但不再是运行时默认读取入口。

## 极低伤害

`M` 是随机前的兵力相关基础 floor，`U` 则是随机后的最终下限候选，两者不能合并：

```text
DamageBase = max(D0(R), U)
U in {5, ..., 15}
```

现有样本支持这个范围和 `max` 结构，但尚不能证明 `U` 是否每次攻击都抽取、是否等概率、或仅在低伤害时触发。因此运行时应将它实现为可配置的随机/候选层，而非固定常数。

## 验证摘要与边界

内部离线复核中：

- 新批骑兵对骑兵有效样本：31,346 / 31,357 直接命中；其余 11 条在中段 `F(N) +/- 1` 内。
- DS V2 纯净样本：58,485 / 58,575 直接命中；允许中段 `F(N) +/- 1` 后覆盖全部样本。
- 另一清洗数据集直接命中率为 99.3401%。各文件尚未全局去重，不能把简单加总当成独立样本数。

这些结果不支持下列过度结论：

- 不应声称恢复了官方源码；中段经验表和高段 `log2` 形态仍是逆向候选。
- 不应把它直接推广为所有 PVP、PVE、城卫、会心或战法附伤公式。
- 不应把未解释误差一律称作随机数；先排查出手时兵力、击杀封顶、状态、属性显示精度和未记录倍率。

## 与当前战斗框架的关系

当前实现已通过 `WeaponBaseDamageFormula` 接入 `DamageSystem`：

- `DamageType.WEAPON` 先计算本文件定义的 `DamageBase`，再由 `DamageRequest.coefficient` 做兵刃战法倍率缩放。
- `UnitRuntime` 已提供公式所需的 `level`、`morale` 和 `troop_type`；武力、统率仍统一通过 `AttributeSystem` 读取最终属性。
- `R` 使用战斗唯一 `RandomSystem` 在 `86..94` 的离散整数中取值；`U` 也通过可配置离散范围生成。
- `TroopSystem` 仍是唯一实际扣兵入口，因此 `DamageSystem` 不重复执行击杀封顶。
- `F(N)` 在 `1..10000` 范围内全部从 `troop_function_table_1_10000.csv` 读取，运行时不再计算低段公式、中段近似或高段 `log2`。
