# 演示场景套件升级规划

> 状态：当前演示升级规划  
> 日期：2026-09-16  
> 起点：第十阶段已经合入 `main`

## 1. 为什么要升级

当前 `demo.py` 主要展示基础战斗流程，已经明显落后于现有运行时能力。

项目现在已经支持：

```text
普通攻击
连击
反击
群攻
分担
连锁
混乱
嘲讽
援护
持续伤害
恢复机会
死亡清理
战斗终结
```

如果演示仍然只表现“双方轮流普通攻击”，就无法直观看出这些能力是否真的能在同一场战斗里协同运行。

## 2. 演示程序的正式定位

演示程序负责：

```text
给人看的端到端战斗展示
+
综合运行冒烟验证
```

演示程序不负责：

```text
代替单元测试
代替回归测试
定义官方机制
定义随机数规则
证明某个机制研究结论
```

正式正确性仍由 `tests/`、机制合同和冻结审计保证。

## 3. 设计原则

每个演示场景必须：

```text
固定随机种子
固定武将属性
固定初始状态
固定效果配置
输出可读战斗日志
输出最终战斗结果
可以重复运行得到相同结果
```

不允许把演示写成“随机跑一局，看起来没报错就算通过”。

## 4. 场景一：基础战斗

展示：

```text
行动顺序
普通攻击
伤害计算
兵力扣除
阵亡
胜负判断
战斗结束
```

用途：保留最小可读基线。

## 5. 场景二：普通攻击反应链

展示：

```text
连击
反击
群攻
分担
连锁
伤害后处理
```

目标：让第九阶段的交叉机制顺序可以被人直接观察。

## 6. 场景三：目标与控制

展示：

```text
混乱
嘲讽
援护
震慑
缴械
```

第十一阶段完成对应状态后扩展。

目标：观察“谁可以行动、攻击谁、目标是否被重定向”。

## 7. 场景四：持续状态

展示：

```text
灼烧
水攻
中毒
溃逃
沙暴
叛逃
```

目标：展示状态施加、刷新、持续结算、来源保留、目标阵亡后清理。

## 8. 场景五：恢复体系

展示：

```text
急救
休整
禁疗
倒戈
攻心
```

其中禁疗、倒戈、攻心在第十一阶段正式完成后接入。

目标：展示伤害后恢复、行动开始恢复、恢复禁止和伤害派生恢复之间的区别。

## 9. 场景六：命中与伤害规则

第十一阶段完成后展示：

```text
规避
抵御
必中
破阵
警戒
会心
奇谋
看破
```

目标：让命中、阻止、公式、防御绕过、暴击类效果的先后顺序可读。

## 10. 场景七：复杂综合战斗

至少组合：

```text
连击
反击
状态伤害
恢复
目标控制
阵亡
战斗终结
```

该场景不是为了穷尽所有组合，而是用于证明多个已经冻结的子系统可以一起运行。

## 11. 第十三阶段以后增加战法场景

随着战法系统接入，逐步新增：

```text
突击战法场景
普通主动战法场景
准备战法场景
多战法组合场景
控制状态阻止战法场景
```

不要在第十一、十二阶段提前把具体战法硬塞进演示。

## 12. 建议代码组织

后续可以把单一 `demo.py` 逐步演化成：

```text
demo.py                   统一入口
examples/
  basic_battle.py
  normal_attack_chain.py
  control_targeting.py
  persistent_states.py
  recovery.py
  damage_rules.py
  complex_battle.py
```

具体目录结构在真正施工前再审计；本文件只冻结“场景化演示”的方向，不强制提前重构目录。

## 13. 与测试的边界

每个演示场景都应该有对应的正式测试保护关键合同，但：

```text
演示输出变化
!= 自动代表机制错误
```

只有当正式合同、测试或冻结语义被破坏时才算回归。

## 14. 当前实施顺序

```text
第十一阶段开始前：确定场景框架
第十一阶段：加入命中、伤害、行动类状态展示
第十二阶段：加入复杂控制和权限类状态展示
第十三阶段：加入突击战法展示
第十四阶段：加入主动战法展示
第十五阶段：加入准备战法展示
```

## 15. 验收目标

当第十二阶段完成时，演示套件应至少能够直观展示：

```text
基础战斗
第九阶段交叉机制
第十阶段持续状态
第十一阶段伤害 / 命中 / 恢复 / 行动状态
第十二阶段复杂控制状态
```

届时演示程序才真正与当前战斗引擎能力相匹配。
